<%= simple_form_for @need_card do |f| %>

    <%= f.input :category, collection: @categories, as: :radio_buttons %>
    <%= f.input :vfeeling, collection: @categories, as: :radio_buttons %>

    <%= f.input :note %>
    <%= f.button :submit %>
<% end %>
